import { DirCurrencyFormatterDirective } from './dir-currency-formatter.directive';

describe('DirCurrencyFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new DirCurrencyFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
